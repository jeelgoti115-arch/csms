# CSMS Backend

Node.js + Express backend using MongoDB.

Quick start:

1. cd backend
2. npm install
3. npm run dev

API endpoints:
- GET /api/services
- POST /api/services
- GET /api/users
- GET /api/appointments
- POST /api/appointments


